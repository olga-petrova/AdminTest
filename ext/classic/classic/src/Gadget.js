/**
 * A common base class between the classic and modern toolkits for
 * a simple widget like component that can act as a child of a container.
 * 
 * @since 6.1.0
 */
Ext.define('Ext.Gadget', {
    extend: 'Ext.Widget'
});