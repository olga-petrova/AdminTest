{
  "name": "exporter",
  "namespace": "Ext",
  "format": "1",
  "version": "6.2.0.526",
  "compatVersion": "6.0.0",
  "creator": "Sencha",
  "summary": "Exports data to various formats like: Excel xml, Excel xlsx, csv, tsv, html etc",
  "detailedDescription": "Utilities and plugins to export data in various formats and download them client-side",
  "type": "code",
  "requires": [
    "core"
  ],
  "framework": "ext",
  "output": {
    "base": "${package.dir}/build/${toolkit.name}/${build.id}",
    "js": "..",
    "sass": ""
  },
  "sass": {
    "namespace": "Ext",
    "etc": [
      "${package.dir}/sass/etc/all.scss",
      "${package.dir}/${toolkit.name}/sass/etc/all.scss"
    ],
    "var": [
      "${package.dir}/sass/var",
      "${package.dir}/${toolkit.name}/sass/var"
    ],
    "src": [
      "${package.dir}/sass/src",
      "${package.dir}/${toolkit.name}/sass/src"
    ]
  },
  "classpath": [
    "${package.dir}/src",
    "${package.dir}/${toolkit.name}/src"
  ],
  "overrides": [
    "${package.dir}/overrides",
    "${package.dir}/${toolkit.name}/overrides"
  ]
}